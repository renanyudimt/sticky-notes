# Ativa o React Scan em desenvolvimento.
# Só tem efeito quando rodando em modo dev (import.meta.env.DEV).
# "true" => abre o React Scan. Ausente ou qualquer outro valor => não abre.
VITE_REACT_SCAN=true
