export { createLocalStorageRepository } from './localStorageRepository';
export { createMockRestRepository } from './mockRestRepository';
export { createRepository } from './createRepository';
export {
  LOCAL_STORAGE_KEY,
  MOCK_REST_LATENCY,
  REST_STORAGE_KEY,
} from './constants';
export type { NotesRepository, RepositoryKind } from './types';
